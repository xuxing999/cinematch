# CineMatch 快速安裝指南

## 🎯 第二階段已完成！

恭喜！專案核心架構已建置完成，現在可以開始安裝並執行專案。

---

## 📦 安裝步驟

### 1. 安裝依賴

在專案根目錄執行：

```bash
npm install
```

或使用其他套件管理工具：

```bash
yarn install
# 或
pnpm install
```

### 2. 設定環境變數

複製環境變數範例檔案：

```bash
cp .env.example .env.local
```

然後編輯 `.env.local`，填入您的實際 API 金鑰：

```bash
# Supabase（從 Supabase Dashboard > Settings > API 取得）
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# TMDB（從 TMDB Settings > API 取得）
NEXT_PUBLIC_TMDB_API_KEY=your-tmdb-api-key-here

# 應用程式 URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### 3. 執行資料庫遷移

請確保您已在 Supabase Dashboard 的 SQL Editor 中執行 `supabase_schema.sql` 腳本。

### 4. 啟動開發伺服器

```bash
npm run dev
```

訪問 http://localhost:3000 即可看到應用程式！

---

## ✅ 驗證安裝

啟動後，您應該能看到：

1. **導航欄**：包含「熱門雷達」、「訊號大廳」、「快閃聊天」、「個人資料」
2. **熱門雷達頁面**：顯示現正熱映的電影海報與訊號數
3. **霓虹深色主題**：黑色背景 (#221f1f) + 紅色強調色 (#e50914)
4. **響應式設計**：桌面版顯示頂部導航，手機版顯示底部導航

如果看到電影海報，恭喜您安裝成功！

---

## 🔍 常見問題

### 問題：無法載入電影海報

**原因**：Next.js 需要配置允許的圖片域名。

**解決方案**：`next.config.js` 已經配置了 TMDB 圖片域名，確認配置檔案正確。

### 問題：Supabase 連線錯誤

**解決方案**：
1. 確認 `.env.local` 中的 Supabase URL 和 Key 正確
2. 確認 Supabase 專案狀態為 Active
3. 檢查是否已執行 SQL 腳本建立資料表

### 問題：匿名登入失敗

**解決方案**：
1. 前往 Supabase Dashboard > Authentication > Providers
2. 啟用 **Anonymous Sign-in**
3. 重新整理頁面

---

## 📂 已建立的檔案清單

### 配置檔案
- `package.json` - 專案依賴與腳本
- `tsconfig.json` - TypeScript 配置
- `tailwind.config.ts` - Tailwind CSS 配置（霓虹主題）
- `next.config.js` - Next.js 配置
- `middleware.ts` - 認證中間件

### 核心功能
- `lib/supabase/` - Supabase 客戶端
- `lib/tmdb/` - TMDB API 客戶端
- `lib/hooks/` - Custom Hooks (useAuth)
- `lib/utils/` - 工具函數 (cn, date)

### UI 組件
- `components/ui/` - Button, Card, Badge, Input, Avatar, Modal
- `components/layout/` - Navbar, BottomNav
- `components/home/` - MovieCard, MovieGrid

### API Routes
- `/api/tmdb/trending` - 取得熱門電影
- `/api/tmdb/now-playing` - 取得現正熱映電影
- `/api/signals` - 訊號 CRUD
- `/api/signals/stats` - 訊號統計

### 頁面
- `/` - 熱門雷達（已完成）
- `/lobby` - 訊號大廳（待建立）
- `/chat` - 快閃聊天（待建立）
- `/profile` - 個人資料（待建立）

---

## 🚀 下一步

第二階段已完成！您現在可以：

1. **測試熱門雷達頁面**：查看電影海報、點擊查看詳情
2. **進入第三階段**：建立訊號大廳、快閃聊天室、個人資料頁面
3. **自定義樣式**：調整 Tailwind 配置中的霓虹色彩

準備好繼續開發了嗎？讓我知道您想要先實作哪個功能！
